name: Reusable • Sync only org labels
on:
  workflow_call: {}

jobs:
  sync-labels:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: write
    steps:
      - name: Checkout standards from nubo-educacao/.github
        uses: actions/checkout@v4
        with:
          repository: nubo-educacao/.github
          ref: main

      - name: Apply only the org labels (no delete)
        uses: crazy-max/ghaction-github-labeler@v5
        with:
          yaml-file: standards/labels.yml
          skip-delete: true # não remove labels existentes
          dry-run: false
